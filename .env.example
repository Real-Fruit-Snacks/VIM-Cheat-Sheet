# GitLab Configuration for Private Pages
# Copy this file to .env and fill in your values

# Your GitLab personal access token (required for private pages)
# Create one at: https://gitlab.com/-/profile/personal_access_tokens
# Required scopes: api, read_repository
GITLAB_TOKEN=your_gitlab_personal_access_token

# Your GitLab Pages URL (for development proxy)
# Format: https://username.gitlab.io/project-name
GITLAB_PAGES_URL=https://your-username.gitlab.io/VIM

# Optional: GitLab instance URL (if using self-hosted GitLab)
GITLAB_API_URL=https://gitlab.com